@page 
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">Se produjo un error al procesar su solicitud.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>ID de solicitud:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Modo de desarrollo</h3>
<p>
    Cambio al <strong>desarrollo</strong> El entorno muestra información detallada sobre el error que se produjo.
</p>
<p>
    <strong>El entorno de desarrollo no debe habilitarse para las aplicaciones implementadas.</strong>
    Puede resultar en mostrar información confidencial de excepciones a los usuarios finales.
    Para la depuración local, habilite el<strong>Desarrollo</strong> medio ambiente estableciendo el <strong>ASPNETCORE_ENVIRONMENT</strong> variable de entorno a <strong>Desarrollo</strong>
    y reiniciando la aplicación.
</p>
